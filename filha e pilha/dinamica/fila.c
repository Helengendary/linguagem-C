#include <stdio.h>
#include "fila.h"

int main() {

    Enqueue(5);
    Enqueue(8);
    Enqueue(2);
    Enqueue(0);
    Enqueue(9);
    Enqueue(1);
    GetSize();
    get(0);
    Dequeue();
    get(0);
    ultimo();
}
